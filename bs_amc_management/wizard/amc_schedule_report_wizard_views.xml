<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_amc_schedule_report_wizard_form" model="ir.ui.view">
        <field name="name">amc.schedule.report.wizard.form</field>
        <field name="model">amc.schedule.report.wizard</field>
        <field name="arch" type="xml">
            <form string="AMC Schedule Report">
                <group>
                    <group>
                        <field name="year"/>
                        <field name="product_categ_id"
                               options="{'no_create': True, 'no_create_edit': True}"/>
                    </group>
                    <group>
                        <field name="site_ids" widget="many2many_tags"
                               options="{'no_create': True, 'no_create_edit': True}"/>
                    </group>
                </group>
                <div class="text-muted">
                    Monthly figures are the AMC provision entries booked in each month of the
                    selected year, by accounting date. Leave a filter empty to include everything.
                </div>
                <footer>
                    <button name="action_generate_report" string="Generate Report" type="object"
                            class="btn-primary" data-hotkey="q"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_amc_schedule_report_wizard" model="ir.actions.act_window">
        <field name="name">AMC Schedule Report</field>
        <field name="res_model">amc.schedule.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
