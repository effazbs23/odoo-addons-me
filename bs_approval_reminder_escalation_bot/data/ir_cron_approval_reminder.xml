<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_approval_reminder_check" model="ir.cron">
            <field name="name">Approval Reminder &amp; Escalation Bot — Daily Check</field>
            <field name="model_id" ref="model_bs_approval_reminder_config"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="state">code</field>
            <field name="code">model._cron_approval_reminder_check()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
        </record>
    </data>
</odoo>