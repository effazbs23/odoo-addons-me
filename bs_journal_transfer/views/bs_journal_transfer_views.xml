<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_bs_journal_transfer_tree" model="ir.ui.view">
        <field name="name">bs.journal.transfer.tree</field>
        <field name="model">bs.journal.transfer</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="date"/>
                <field name="source_journal_id"/>
                <field name="destination_journal_id"/>
                <field name="amount" widget="monetary"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="state" widget="badge"
                       decoration-info="state=='draft'"
                       decoration-success="state=='confirmed'"
                       decoration-danger="state=='cancelled'"
                />
            </list>
        </field>
    </record>

    <record id="view_bs_journal_transfer_form" model="ir.ui.view">
        <field name="name">bs.journal.transfer.form</field>
        <field name="model">bs.journal.transfer</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" string="Confirm" type="object"
                            invisible="state != 'draft'" class="oe_highlight"/>
                    <button name="action_cancel" string="Cancel" type="object"
                            invisible="state in ['cancelled', 'confirmed']"/>
                    <button name="action_draft" string="Reset to Draft" type="object"
                            invisible="state != 'cancelled'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="action_view_statement_lines"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-list"
                            invisible="statement_count == 0">
                            <field name="statement_count" widget="statinfo" string="Statement Lines"/>
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="date" readonly="state != 'draft'"/>
                            <field name="source_journal_id" readonly="state != 'draft'"/>
                            <field name="amount" widget="monetary" readonly="state != 'draft'"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="deposit_slip" filename="deposit_slip_filename"
                                   readonly="state == 'confirmed'" string="Attachment"/>
                            <field name="deposit_slip_filename" invisible="1"/>
                        </group>
                        <group>
                            <field name="company_id" readonly="1"/>
                            <field name="destination_journal_id" readonly="state != 'draft'"/>
                            <field name="submitted_by" readonly="1"/>

                        </group>
                    </group>

                    <group>
                        <field name="notes" placeholder="Additional notes..." readonly="state == 'confirmed'"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_bs_journal_transfer" model="ir.actions.act_window">
        <field name="name">Bank Cash Transfer</field>
        <field name="res_model">bs.journal.transfer</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
