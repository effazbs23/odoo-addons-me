<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="simple_invoicing_group" model="res.groups">
        <field name="name">Simple Invoicing User</field>
        <field name="comment">Sees the simplified Invoicing menu, invoice form and status funnel instead of the full Accounting app. Does not grant or remove any access rights -- it only changes what is shown. A user also in the Accounting Administrator group always sees the full view regardless of this group.</field>
        <field name="implied_ids" eval="[(4, ref('account.group_account_invoice'))]"/>
    </record>
</odoo>
