<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.bs_smart_kpi_dashboard</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="50"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Smart KPI Dashboard" string="Smart KPI Dashboard"
                     name="bs_smart_kpi_dashboard" groups="base.group_system">
                    <block title="Vocabulary Growth" id="kpi_dashboard_vocabulary">
                        <setting id="kpi_dashboard_fuzzy_match_setting"
                                 help="Off by default — parsing behaves exactly as it does today until this is switched on.">
                            <field name="kpi_dashboard_fuzzy_match_enabled"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_bs_smart_kpi_dashboard_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'module': 'bs_smart_kpi_dashboard'}</field>
    </record>

</odoo>
