<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_mrp_bom_form_variant_matrix" model="ir.ui.view">
        <field name="name">mrp.bom.form.variant.matrix</field>
        <field name="model">mrp.bom</field>
        <field name="inherit_id" ref="mrp.mrp_bom_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <header>
                    <button name="action_open_variant_matrix" type="object"
                            string="Variant Matrix" class="btn-secondary"
                            invisible="not product_tmpl_id"/>
                </header>
            </xpath>
        </field>
    </record>
</odoo>
