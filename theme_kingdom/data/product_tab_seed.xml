<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Runs on every module update so Featured / defaults exist after upgrades. -->
    <function model="kingdom.product.tab" name="ensure_default_tabs"/>
</odoo>
