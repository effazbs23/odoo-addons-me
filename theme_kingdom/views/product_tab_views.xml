<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="kingdom_product_tab_list" model="ir.ui.view">
        <field name="name">kingdom.product.tab.list</field>
        <field name="model">kingdom.product.tab</field>
        <field name="arch" type="xml">
            <list string="Product Tabs">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="tab_type"/>
                <field name="product_source"/>
                <field name="product_count"/>
                <field name="product_limit" optional="hide"/>
                <field name="category_id" optional="hide"/>
                <field name="show_in_dynamic_tabs" widget="boolean_toggle"/>
                <field name="show_in_product_carousel" widget="boolean_toggle" optional="hide"/>
                <field name="show_in_homepage" widget="boolean_toggle" optional="hide"/>
                <field name="show_in_header_menu" widget="boolean_toggle" optional="hide"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="kingdom_product_tab_form" model="ir.ui.view">
        <field name="name">kingdom.product.tab.form</field>
        <field name="model">kingdom.product.tab</field>
        <field name="arch" type="xml">
            <form string="Product Tab">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_products"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-shopping-cart">
                            <field name="product_count" widget="statinfo" string="Products"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <label for="name" string="Tab Name"/>
                        <h1>
                            <field name="name" placeholder="e.g. Featured, New Arrivals, Best Sellers"/>
                        </h1>
                    </div>
                    <group>
                        <group string="General">
                            <field name="sequence"/>
                            <field name="active"/>
                            <field name="tab_type"/>
                            <field name="product_source"/>
                            <field name="category_id"
                                     invisible="product_source != 'auto' or tab_type != 'category'"
                                     required="product_source == 'auto' and tab_type == 'category'"/>
                            <field name="product_limit"
                                     invisible="product_source == 'manual'"/>
                        </group>
                        <group string="Where to show">
                            <field name="show_in_dynamic_tabs"/>
                            <field name="show_in_product_carousel"
                                     invisible="tab_type not in ('new_arrival', 'best_seller')"/>
                            <field name="show_in_homepage"/>
                            <field name="show_in_header_menu"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Products" name="products">
                            <field name="product_ids"
                                     invisible="product_source != 'manual'"
                                     mode="list,kanban"
                                     context="{'default_sale_ok': True, 'default_is_published': True}">
                                <list>
                                    <field name="name"/>
                                    <field name="default_code" optional="show"/>
                                    <field name="list_price"/>
                                    <field name="is_published"/>
                                </list>
                            </field>
                            <div invisible="product_source != 'auto'" class="text-muted fst-italic p-2">
                                Products are loaded automatically from Tab Type
                                (New Arrivals, Best Sellers, Featured, On Sale, or Category).
                                Switch Product Source to <strong>Manual selection</strong> to pick products yourself.
                            </div>
                        </page>
                        <page string="Feature Banner" name="banner">
                            <group>
                                <group>
                                    <field name="feature_image" widget="image" class="oe_avatar"/>
                                </group>
                                <group>
                                    <field name="feature_label"/>
                                    <field name="feature_url" placeholder="/shop or https://..."/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="kingdom_product_tab_action" model="ir.actions.act_window">
        <field name="name">Product Tabs</field>
        <field name="res_model">kingdom.product.tab</field>
        <field name="view_mode">list,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create product tabs (Featured, New Arrivals, Best Sellers, …).
            </p>
            <p>
                Choose <em>Automatic</em> to fill products by type, or <em>Manual selection</em>
                to pick products yourself. Enable <strong>Dynamic Product Tabs</strong> so the
                website snippet can load them with AJAX tab switching.
            </p>
        </field>
    </record>

    <menuitem id="kingdom_product_tab_menu"
              name="Product Tabs"
              parent="website.menu_website_global_configuration"
              action="kingdom_product_tab_action"
              groups="website.group_website_designer"
              sequence="51"/>

</odoo>
