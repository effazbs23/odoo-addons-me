<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_template_form_view_brand" model="ir.ui.view">
        <field name="name">product.template.form.kingdom.brand</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <field name="categ_id" position="after">
                <field name="kingdom_brand_id"
                         options="{'no_quick_create': True}"
                         placeholder="Select brand"/>
            </field>
        </field>
    </record>

    <record id="product_template_tree_view_brand" model="ir.ui.view">
        <field name="name">product.template.list.kingdom.brand</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view"/>
        <field name="arch" type="xml">
            <field name="categ_id" position="after">
                <field name="kingdom_brand_id" optional="show"/>
            </field>
        </field>
    </record>

</odoo>
